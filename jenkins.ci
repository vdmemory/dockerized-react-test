pipeline {
    agent any

    stages {
        stage('Build docker image. Build app.') {
            steps {
                git 'https://github.com/vdmemory/dockerized-react-test.git'
                sh './build.sh'
            }
        }
    }
}